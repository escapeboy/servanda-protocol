{
  "name": "servanda-vectors-gen",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic generator for the Servanda protocol conformance test vectors.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc",
    "generate": "npm run build && node dist/generate.js",
    "selfcheck": "npm run build && node dist/selfcheck.js",
    "validate": "npm run build && node dist/validate.js",
    "test": "npm run build && node dist/selfcheck.js && node dist/validate.js"
  },
  "dependencies": {
    "@noble/curves": "^1.6.0",
    "@noble/hashes": "^1.5.0",
    "@scure/bip39": "^1.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "typescript": "^5.6.3"
  }
}
